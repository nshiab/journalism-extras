{
  "name": "@nshiab/journalism-extras",
  "version": "1.0.8",
  "description": "Useful TypeScript functions for journalism projects. This library holds any functions and classes that do not fit in the other journalism modules.",
  "repository": "https://github.com/nshiab/journalism-extras",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts",
    "./web": "./src/web.ts"
  },
  "tasks": {
    "all-tests": "deno install && deno fmt --check && deno lint && deno check src/index.ts && deno doc --lint src/index.ts && deno publish --allow-dirty --dry-run && TZ=America/Toronto deno test -A --fail-fast && deno task llm",
    "llm": "deno run -A jsr:@nshiab/deno-docs-to-md && deno fmt llm.md",
    "test-coverage": "deno test -A --fail-fast --coverage=cov_profile && deno coverage cov_profile",
    "patch": "deno task all-tests && deno run -A jsr:@nshiab/bump-version patch",
    "minor": "deno task all-tests && deno run -A jsr:@nshiab/bump-version minor",
    "major": "deno task all-tests && deno run -A jsr:@nshiab/bump-version major"
  },
  "exclude": [
    "npm/"
  ],
  "publish": {
    "exclude": [
      "test",
      ".github",
      "npm"
    ]
  },
  "nodeModulesDir": "auto",
  "imports": {
    "@nshiab/journalism-format": "jsr:@nshiab/journalism-format@^1.1.7",
    "adm-zip": "npm:adm-zip@^0.5.17",
    "iconv-lite": "npm:iconv-lite@^0.7.2"
  },
  "compilerOptions": {
    "lib": [
      "dom",
      "deno.ns"
    ]
  },
  "lint": {
    "include": [
      "src/**/*.ts"
    ]
  }
}
